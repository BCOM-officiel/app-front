import { Box, Container } from "@mui/material";

const Hero = () => {
  return (
    <Box
      sx={{
        backgroundImage: "linear-gradient(to bottom, #111, #333)",
        color: "#fff",
        py: "400px",
      }}
    >
      <Container maxWidth="xl">
      </Container>
    </Box>
  );
};

export default Hero;
